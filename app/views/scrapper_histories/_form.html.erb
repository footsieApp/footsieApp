<%= form_for(@scrapper_history) do |f| %>
  <% if @scrapper_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scrapper_history.errors.count, "error") %> prohibited this scrapper_history from being saved:</h2>

      <ul>
      <% @scrapper_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
